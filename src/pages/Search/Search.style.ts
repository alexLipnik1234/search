import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  SearchPageContainer: {
    paddingTop: "40px",
    height: "100%",
    width: "100%",
    display: "flex",
    justifyContent: "center",
  },
});
